mutation createUser($input: UserInput!) {
    createUser(input: $input) {
        id,
        firstName,
        lastName
    }
}

# Variables
{
    "input": {
        "firstName": "Jeff",
        "lastName": "Bytezos"
    }
}