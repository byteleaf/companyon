mutation CreateTask($input: TaskInput!) {
    createTask(input: $input) {
        id
        user {
            id
            firstName,
            lastName,
            email
        }
        description
        status
    }
}