mutation ApproveAbsenceRequest($id: ID!, $approved: Boolean!) {
    approveAbsenceRequest(id: $id, approved: $approved) {
        id,
        description,
        approval {
            user,
            timeStamp
        }
    }
}