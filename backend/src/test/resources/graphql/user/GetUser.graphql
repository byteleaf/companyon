query GetUser($id: ID!) {
    user(id: $id) {
        id
        firstName,
        lastName,
        email,
        weeklyWorkingMinutes,
        vacationDaysPerYear,
        province,
        country
    }
}